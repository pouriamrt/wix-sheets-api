# Copy to .env and fill in. Used by docker compose (env_file).
# Keep .env out of version control.

SHEET_ID=
DEFAULT_RANGE=Sheet1!A:Z
ENTRIES_RANGE=New Entries to Verify!A:K
COMMENTS_RANGE=Comments!A:B
